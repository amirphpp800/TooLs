# Cloudflare Pages redirects and rewrites

# Fix asset paths - redirect incorrect /pages/assets/* to /assets/*
/pages/assets/* /assets/:splat 301

# Fix pages routing
/pages/* /:splat 200

# Fallback for SPA routing
/* /index.html 200
